
  # Chatbot UI Features

  This is a code bundle for Chatbot UI Features. The original project is available at https://www.figma.com/design/XGdzJ2t1HPabG2avThGYBV/Chatbot-UI-Features.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  